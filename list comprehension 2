# Original list of numbers
numbers = [2, 5, 12, 8, 15, 7, 20]

# List comprehension with a simple condition
# For each number:
# - If number > 10 → label it "big"
# - Otherwise → label it "small"
size_label1 = ["big" if num > 10 else "small" for num in numbers]
print(size_label1)

# List comprehension with multiple conditions (nested ternary)
# For each number:
# - If number > 10 → "big"
# - Else if number >= 5 → "medium"
# - Else → "small"
size_label2 = [
    "big" if num > 10 
    else "medium" if num >= 5 
    else "small"
    for num in numbers
]

print(size_label2)
# Output: ['small', 'medium', 'big', 'medium', 'big', 'medium', 'big']

# Nested if with List Comprehension
basket = ['apple', 'guava', 'cherry', 'banana']
my_fruits = ['apple', 'kiwi', 'grapes', 'banana']

# Add new list from my_fruits and items if the fruit exists in basket and also starts with 'a'
[fruit for fruit in my_fruits if fruit in basket and fruit.startswith('a')]

# The result is ['apple'] because apple is the only fruit in my_fruits that exists in the basket and starts with 'a'.


